const { getDefaultConfig } = require('expo/metro-config');

/**
 * Metro Bundler Config — OneBuddy Customer Mobile App
 * Port: 8081 (passed via CLI: expo start --port 8081)
 */
module.exports = getDefaultConfig(__dirname);

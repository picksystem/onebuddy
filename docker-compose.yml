version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: bandi-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: travelmate_db
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - bandi-network

  redis:
    image: redis:7-alpine
    container_name: bandi-redis
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    networks:
      - bandi-network

  # Uncomment to run the admin in Docker
  # admin:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: bandi-admin
  #   environment:
  #     - APP=admin
  #     - DATABASE_URL=postgresql://postgres:password@postgres:5432/travelmate_db?schema=public
  #     - REDIS_HOST=redis
  #     - REDIS_PORT=6379
  #   ports:
  #     - '3001:3001'
  #   depends_on:
  #     - postgres
  #     - redis
  #   networks:
  #     - bandi-network

  # Uncomment to run the user in Docker
  # user:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: bandi-user
  #   environment:
  #     - APP=user
  #     - DATABASE_URL=postgresql://postgres:password@postgres:5432/travelmate_db?schema=public
  #     - REDIS_HOST=redis
  #     - REDIS_PORT=6379
  #   ports:
  #     - '3002:3002'
  #   depends_on:
  #     - postgres
  #     - redis
  #   networks:
  #     - bandi-network

volumes:
  postgres_data:
  redis_data:

networks:
  bandi-network:
    driver: bridge
